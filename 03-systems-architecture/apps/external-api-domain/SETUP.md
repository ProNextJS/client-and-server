This example requires domain mapping. `mycompany.com/*` needs to be mapped to `http://localhost:3005/*` and `api.mycompany.com/*` needs to be mapped to `http://localhost:5001/*`.

![Domain Mapping](./images/remote-mapping.png)
